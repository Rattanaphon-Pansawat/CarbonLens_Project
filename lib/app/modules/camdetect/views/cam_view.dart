import 'package:flutter/material.dart';

class CameView extends StatelessWidget {
  const CameView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CameView'),
      ),
    );
  }
}
