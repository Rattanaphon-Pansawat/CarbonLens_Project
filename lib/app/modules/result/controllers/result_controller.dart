import 'package:get/get.dart';

class ResultController extends GetxController {
  // ใช้สำหรับเก็บสถานะ, ตัวแปร, และฟังก์ชันของกล้อง/การตรวจจับในอนาคต

  @override
  void onInit() {
    super.onInit();
    // โค้ดเริ่มต้น เช่นการเตรียมข้อมูล
  }

  @override
  void onClose() {
    super.onClose();
    // ทำความสะอาด resource เมื่อ controller ถูกลบ
  }
}
